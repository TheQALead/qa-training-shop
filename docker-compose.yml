services:
  qa-shop:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qa-training-shop
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/db/custom.db
    volumes:
      # Persist database
      - qa-shop-db:/app/db
    restart: on-failure:5
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/init"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

volumes:
  qa-shop-db:
    driver: local
